---
import Layout from '../layouts/Layout.astro';
import BentoBox from '../components/BentoBox.astro';
import { getCollection } from 'astro:content';

const photoEntries = await getCollection('photography');
// Pega apenas ensaios COM data, ordenados do mais recente para o mais antigo
const latestPhoto = photoEntries
  .filter(e => e.data.date)
  .sort((a, b) => b.data.date!.valueOf() - a.data.date!.valueOf())[0];

const dataEntries = await getCollection('data-engineering');
const latestDataProject = dataEntries[0];

const diceEntries = await getCollection('dice-awards');
const latestDice = diceEntries[0];

const academicEntries = await getCollection('academic');
const latestAcademic = academicEntries[0];

const blogEntries = await getCollection('blog');
const latestBlog = blogEntries.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())[0];
---

<Layout title="Lucas Ramos | Portfolio">
  <h1 class="sr-only">Lucas Ramos</h1>

  <main>
    <div class="bento-grid">
      
      <!-- Welcome / About Box -->
      <BentoBox spanCol="col-span-4 md:col-span-2" spanRow="row-span-2" glowColor="cyan" href="/about" className="flex flex-col justify-end group">
        <div class="absolute inset-0 bg-gradient-to-br from-surface to-base opacity-50 group-hover:opacity-30 transition-opacity"></div>
        <div class="relative z-10">
          <span class="font-mono text-xs text-neonLime tracking-widest mb-3 block">✦ lucasramos.dev</span>
          <h2 class="text-2xl font-bold mb-4 pt-only">oi, eu sou o <span class="text-neonLime">Lucas</span>.</h2>
          <h2 class="text-2xl font-bold mb-4 en-only">hi, I'm <span class="text-neonLime">Lucas</span>.</h2>
          <h2 class="text-2xl font-bold mb-4 fr-only">salut, je suis <span class="text-neonLime">Lucas</span>.</h2>
          <h2 class="text-2xl font-bold mb-4 es-only">hola, soy <span class="text-neonLime">Lucas</span>.</h2>
          <h2 class="text-2xl font-bold mb-4 ja-only">こんにちは、<span class="text-neonLime">ルカス</span>です。</h2>
          <h2 class="text-2xl font-bold mb-4 zh-only">你好，我是 <span class="text-neonLime">Lucas</span>。</h2>
          <h2 class="text-2xl font-bold mb-4 de-only">hi, ich bin <span class="text-neonLime">Lucas</span>.</h2>
          <h2 class="text-2xl font-bold mb-4 ru-only">привет, я <span class="text-neonLime">Лукас</span>.</h2>
          
          <div class="text-textMuted text-sm mb-6 space-y-3 pt-only">
            <p>Esse é o meu cantinho da internet. Aqui do lado tem as <span class="text-neonLime/80">fotos</span> que eu não sinto vergonha de compartilhar e abaixo tem os <span class="text-neonLime/80">artigos</span> que me forço tanto a escrever quanto a postar.</p>
            <p>Não sei como você parou aqui, mas não espalha: se eu tiver muitos acessos começo a pagar o armazenamento das fotos.</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 en-only">
            <p>This is my little corner of the internet. Over there are the <span class="text-neonLime/80">photos</span> I'm not embarrassed to share, and below are the <span class="text-neonLime/80">articles</span> I force myself to both write and actually post.</p>
            <p>Not sure how you ended up here, but keep it quiet — if I get too many visitors I'll have to start paying for photo storage.</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 fr-only">
            <p>C'est mon petit coin d'internet. À côté il y a les <span class="text-neonLime/80">photos</span> dont je n'ai pas honte de partager, et en bas les <span class="text-neonLime/80">articles</span> que je me force autant à écrire qu'à publier.</p>
            <p>Je sais pas trop comment tu es arrivé ici, mais garde ça pour toi : si j'ai trop de visites je vais devoir payer le stockage des photos.</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 es-only">
            <p>Este es mi pequeño rincón de internet. Aquí al lado están las <span class="text-neonLime/80">fotos</span> que no me da vergüenza compartir, y debajo están los <span class="text-neonLime/80">artículos</span> que me obligo a escribir y publicar.</p>
            <p>No estoy seguro de cómo terminaste aquí, pero no lo digas: si recibo demasiadas visitas tendré que empezar a pagar por el almacenamiento de las fotos.</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 ja-only">
            <p>これは私のインターネット上の小さなコーナーです。あちらには共有しても恥ずかしくない<span class="text-neonLime/80">写真</span>があり、下には無理やり書いて投稿した<span class="text-neonLime/80">記事</span>があります。</p>
            <p>どうやってここにたどり着いたのか分かりませんが、内緒にしてください。訪問者が多すぎると、写真のストレージ料金を払い始めなければならなくなります。</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 zh-only">
            <p>这是我在互联网上的一个小角落。旁边是我不以为耻分享的<span class="text-neonLime/80">照片</span>，下面是我强迫自己撰写并发布的<span class="text-neonLime/80">文章</span>。</p>
            <p>不知道你是怎么来到这里的，但请保持低调：如果访客太多，我就不得不开始支付照片存储费用了。</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 de-only">
            <p>Das ist meine kleine Ecke im Internet. Dort drüben sind die <span class="text-neonLime/80">Fotos</span>, die mir nicht peinlich sind zu teilen, und unten sind die <span class="text-neonLime/80">Artikel</span>, die ich mich zwinge zu schreiben und tatsächlich zu posten.</p>
            <p>Nicht sicher, wie du hier gelandet bist, aber behalte es für dich — wenn ich zu viele Besucher bekomme, muss ich anfangen für den Fotospeicher zu bezahlen.</p>
          </div>
          <div class="text-textMuted text-sm mb-6 space-y-3 ru-only">
            <p>Это мой маленький уголок в интернете. Там <span class="text-neonLime/80">фотографии</span>, которыми мне не стыдно поделиться, а ниже — <span class="text-neonLime/80">статьи</span>, которые я заставляю себя писать и публиковать.</p>
            <p>Не знаю, как вы здесь оказались, но никому не говорите: если будет слишком много посетителей, мне придется начать платить за хранение фотографий.</p>
          </div>
          
          <div class="flex items-center text-neonCyan font-mono text-xs group-hover:translate-x-1 transition-transform">
            <span class="pt-only">sobre mim &rarr;</span>
            <span class="en-only">about me &rarr;</span>
            <span class="fr-only">à propos &rarr;</span>
            <span class="es-only">sobre mí &rarr;</span>
            <span class="ja-only">私について &rarr;</span>
            <span class="zh-only">关于我 &rarr;</span>
            <span class="de-only">über mich &rarr;</span>
            <span class="ru-only">обо мне &rarr;</span>
          </div>
        </div>
      </BentoBox>

      <!-- Latest Photo Box -->
      <BentoBox spanCol="col-span-4 md:col-span-2" spanRow="row-span-2" glowColor="lime" href="/photography" noPadding={true}>
        {latestPhoto && latestPhoto.data.gallery && latestPhoto.data.gallery.length > 0 ? (
          <div class="relative w-full h-full min-h-[300px] flex flex-col justify-end p-6">
            <div class="absolute inset-0 transition-all duration-500 z-0 overflow-hidden">
              <img 
                src={latestPhoto.data.gallery[0].src} 
                alt={latestPhoto.data.gallery[0].caption || latestPhoto.data.title} 
                class="w-full h-full object-cover group-hover:scale-105 transition-all duration-700"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent pointer-events-none"></div>
            </div>
            
            <div class="relative z-20 pointer-events-none">
              <span class="text-xs font-mono text-neonLime mb-1 block pt-only">ÚLTIMA FOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block en-only">LATEST PHOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block fr-only">DERNIÈRE PHOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block es-only">ÚLTIMA FOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ja-only">最新の写真</span>
              <span class="text-xs font-mono text-neonLime mb-1 block zh-only">最新照片</span>
              <span class="text-xs font-mono text-neonLime mb-1 block de-only">LETZTES FOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ru-only">ПОСЛЕДНЕЕ ФОТО</span>
              <h3 class="text-xl font-bold text-gray-100 pt-only">{latestPhoto.data.gallery[0].caption || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 en-only">{latestPhoto.data.gallery[0].captionEn || latestPhoto.data.titleEn || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 fr-only">{latestPhoto.data.gallery[0].captionFr || latestPhoto.data.titleFr || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 es-only">{latestPhoto.data.gallery[0].captionEs || latestPhoto.data.titleEs || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 ja-only">{latestPhoto.data.gallery[0].captionJa || latestPhoto.data.titleJa || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 zh-only">{latestPhoto.data.gallery[0].captionZh || latestPhoto.data.titleZh || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 de-only">{latestPhoto.data.gallery[0].captionDe || latestPhoto.data.titleDe || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 ru-only">{latestPhoto.data.gallery[0].captionRu || latestPhoto.data.titleRu || latestPhoto.data.title}</h3>
              
              <div class="flex gap-2 text-[10px] font-mono text-gray-300 mt-3 flex-wrap">
                {latestPhoto.data.date && (
                  <>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded pt-only">{latestPhoto.data.date.toLocaleDateString('pt-BR')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded en-only">{latestPhoto.data.date.toLocaleDateString('en-US')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded fr-only">{latestPhoto.data.date.toLocaleDateString('fr-FR')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded es-only">{latestPhoto.data.date.toLocaleDateString('es-ES')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded ja-only">{latestPhoto.data.date.toLocaleDateString('ja-JP')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded zh-only">{latestPhoto.data.date.toLocaleDateString('zh-CN')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded de-only">{latestPhoto.data.date.toLocaleDateString('de-DE')}</span>
                    <span class="bg-black/50 border border-white/10 px-2 py-1 rounded ru-only">{latestPhoto.data.date.toLocaleDateString('ru-RU')}</span>
                  </>
                )}
                {latestPhoto.data.gallery[0].camera && <span class="bg-black/50 border border-white/10 px-2 py-1 rounded">{latestPhoto.data.gallery[0].camera}</span>}
                {latestPhoto.data.gallery[0].lens && <span class="bg-black/50 border border-white/10 px-2 py-1 rounded">{latestPhoto.data.gallery[0].lens}</span>}
                {latestPhoto.data.gallery[0].aperture && <span class="bg-black/50 border border-white/10 px-2 py-1 rounded">{latestPhoto.data.gallery[0].aperture}</span>}
                {latestPhoto.data.gallery[0].iso && <span class="bg-black/50 border border-white/10 px-2 py-1 rounded">ISO {latestPhoto.data.gallery[0].iso}</span>}
                {latestPhoto.data.gallery[0].shutterSpeed && <span class="bg-black/50 border border-white/10 px-2 py-1 rounded">{latestPhoto.data.gallery[0].shutterSpeed}</span>}
              </div>
            </div>
          </div>
        ) : latestPhoto ? (
          <div class="relative w-full h-full min-h-[300px] flex flex-col justify-end p-6">
            <div class="absolute inset-0 transition-all duration-500 z-0 overflow-hidden">
              <img src={latestPhoto.data.cover} alt={latestPhoto.data.title} class="w-full h-full object-cover group-hover:scale-105 transition-all duration-700" loading="lazy" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent pointer-events-none"></div>
            </div>
            <div class="relative z-20 pointer-events-none">
              <span class="text-xs font-mono text-neonLime mb-1 block pt-only">ÚLTIMA FOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block en-only">LATEST PHOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block fr-only">DERNIÈRE PHOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block es-only">ÚLTIMA FOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ja-only">最新の写真</span>
              <span class="text-xs font-mono text-neonLime mb-1 block zh-only">最新照片</span>
              <span class="text-xs font-mono text-neonLime mb-1 block de-only">LETZTES FOTO</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ru-only">ПОСЛЕДНЕЕ ФОТО</span>
              <h3 class="text-xl font-bold text-gray-100 pt-only">{latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 en-only">{latestPhoto.data.titleEn || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 fr-only">{latestPhoto.data.titleFr || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 es-only">{latestPhoto.data.titleEs || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 ja-only">{latestPhoto.data.titleJa || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 zh-only">{latestPhoto.data.titleZh || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 de-only">{latestPhoto.data.titleDe || latestPhoto.data.title}</h3>
              <h3 class="text-xl font-bold text-gray-100 ru-only">{latestPhoto.data.titleRu || latestPhoto.data.title}</h3>
            </div>
          </div>
        ) : (
          <div class="flex flex-col h-full justify-center items-center">
             <svg class="w-8 h-8 text-surfaceHighlight group-hover:text-neonLime transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain pt-only">VER GALERIA</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain en-only">VIEW GALLERY</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain fr-only">VOIR GALERIE</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain es-only">VER GALERÍA</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ja-only">ギャラリーを見る</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain zh-only">查看图库</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain de-only">GALERIE ANSEHEN</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ru-only">ПОСМОТРЕТЬ ГАЛЕРЕЮ</span>
          </div>
        )}
      </BentoBox>

      <!-- Latest Data Engineering Project -->
      <BentoBox spanCol="col-span-4 md:col-span-2" spanRow="row-span-1" glowColor="cyan" href="/data-engineering">
        {latestDataProject ? (
          <div class="flex flex-col h-full justify-between">
            <div>
              <span class="text-xs font-mono text-neonCyan mb-1 block pt-only">ÚLTIMO PROJETO DE ENGENHARIA DE DADOS</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block en-only">LATEST DATA ENGINEERING PROJECT</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block fr-only">DERNIER PROJET D'INGÉNIERIE DES DONNÉES</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block es-only">ÚLTIMO PROYECTO DE INGENIERÍA DE DATOS</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block ja-only">最新のデータエンジニアリングプロジェクト</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block zh-only">最新数据工程项目</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block de-only">NEUESTES DATENPROJEKT</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block ru-only">ПОСЛЕДНИЙ ПРОЕКТ ПО ДАННЫМ</span>
              <h3 class="font-bold text-lg mb-2 pt-only mt-2">{latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 en-only mt-2">{latestDataProject.data.titleEn || latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 fr-only mt-2">{latestDataProject.data.titleFr || latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 es-only mt-2">{latestDataProject.data.titleEs || latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 ja-only mt-2">{latestDataProject.data.titleJa || latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 zh-only mt-2">{latestDataProject.data.titleZh || latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 de-only mt-2">{latestDataProject.data.titleDe || latestDataProject.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 ru-only mt-2">{latestDataProject.data.titleRu || latestDataProject.data.title}</h3>
            </div>
            <div class="flex flex-wrap gap-2 mt-3">
              {latestDataProject.data.technologies.map((tech: string) => (
                <span class="text-[10px] font-mono border border-surfaceHighlight px-2 py-1 rounded-full text-textMuted">
                  {tech}
                </span>
              ))}
            </div>
          </div>
        ) : (
          <div class="flex flex-col h-full justify-center items-center">
             <svg class="w-8 h-8 text-surfaceHighlight group-hover:text-neonCyan transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 002-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path></svg>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain pt-only">VER PROJETOS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain en-only">VIEW PROJECTS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain fr-only">VOIR PROJETS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain es-only">VER PROYECTOS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ja-only">プロジェクトを見る</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain zh-only">查看项目</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain de-only">PROJEKTE ANSEHEN</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ru-only">ПОСМОТРЕТЬ ПРОЕКТЫ</span>
          </div>
        )}
      </BentoBox>

      <!-- Latest DICE Review -->
      <BentoBox spanCol="col-span-2 md:col-span-1" spanRow="row-span-1" glowColor="lime" href="/dice-awards" noPadding={!!latestDice?.data?.cover} className="group overflow-hidden">
        {latestDice?.data?.cover ? (
          <div class="relative h-full w-full min-h-[150px]">
             <div class="absolute inset-0 z-0">
                <img src={latestDice.data.cover} alt={latestDice.data.game} class="w-full h-full object-cover opacity-60 group-hover:opacity-100 group-hover:scale-105 transition-all duration-700" loading="lazy" />
                <div class="absolute inset-0 bg-gradient-to-t from-base via-base/80 to-transparent pointer-events-none"></div>
             </div>
             <div class="relative z-10 p-6 flex flex-col h-full justify-between">
               <div>
                  <span class="text-xs font-mono text-neonLime mb-1 block pt-only">ÚLTIMA REVIEW</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block en-only">LATEST REVIEW</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block fr-only">DERNIÈRE CRITIQUE</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block es-only">ÚLTIMA RESEÑA</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block ja-only">最新のレビュー</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block zh-only">最新评论</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block de-only">LETZTE BEWERTUNG</span>
                  <span class="text-xs font-mono text-neonLime mb-1 block ru-only">ПОСЛЕДНИЙ ОБЗОР</span>
                  <h3 class="font-bold text-sm truncate text-gray-100 mt-2">{latestDice.data.game}</h3>
               </div>
               <div class="mt-4 flex items-end justify-between">
                  <span class="text-2xl font-bold text-gray-100">{latestDice.data.rating}<span class="text-xs text-gray-400">/10</span></span>
                  <span class="text-xs text-gray-300 font-mono">{latestDice.data.year}</span>
               </div>
             </div>
          </div>
        ) : latestDice ? (
          <div class="flex flex-col h-full justify-between">
            <div>
              <span class="text-xs font-mono text-neonLime mb-1 block pt-only">ÚLTIMA REVIEW</span>
              <span class="text-xs font-mono text-neonLime mb-1 block en-only">LATEST REVIEW</span>
              <span class="text-xs font-mono text-neonLime mb-1 block fr-only">DERNIÈRE CRITIQUE</span>
              <span class="text-xs font-mono text-neonLime mb-1 block es-only">ÚLTIMA RESEÑA</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ja-only">最新のレビュー</span>
              <span class="text-xs font-mono text-neonLime mb-1 block zh-only">最新评论</span>
              <span class="text-xs font-mono text-neonLime mb-1 block de-only">LETZTE BEWERTUNG</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ru-only">ПОСЛЕДНИЙ ОБЗОР</span>
              <h3 class="font-bold text-sm truncate mt-2">{latestDice.data.game}</h3>
            </div>
            <div class="mt-4 flex items-end justify-between">
              <span class="text-2xl font-bold">{latestDice.data.rating}<span class="text-xs text-textMuted">/10</span></span>
              <span class="text-xs text-textMuted font-mono">{latestDice.data.year}</span>
            </div>
          </div>
        ) : (
          <div class="flex flex-col h-full justify-center items-center">
             <svg class="w-8 h-8 text-surfaceHighlight group-hover:text-neonLime transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 4a2 2 0 114 0v1a1 1 0 001 1h3a1 1 0 011 1v3a1 1 0 01-1 1h-1a2 2 0 100 4h1a1 1 0 011 1v3a1 1 0 01-1 1h-3a1 1 0 01-1-1v-1a2 2 0 10-4 0v1a1 1 0 01-1 1H7a1 1 0 01-1-1v-3a1 1 0 00-1-1H4a2 2 0 110-4h1a1 1 0 001-1V7a1 1 0 011-1h3a1 1 0 001-1V4z"></path></svg>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain pt-only">VER REVIEWS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain en-only">VIEW REVIEWS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain fr-only">VOIR CRITIQUES</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain es-only">VER RESEÑAS</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ja-only">レビューを見る</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain zh-only">查看评论</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain de-only">BEWERTUNGEN ANSEHEN</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ru-only">ПОСМОТРЕТЬ ОБЗОРЫ</span>
          </div>
        )}
      </BentoBox>

      <!-- Academic Box -->
      <BentoBox spanCol="col-span-2 md:col-span-1" spanRow="row-span-1" glowColor="cyan" href="/academic">
        {latestAcademic ? (
          <div class="flex flex-col h-full justify-between">
            <div>
              <span class="text-xs font-mono text-neonCyan mb-1 block pt-only">ÚLTIMO ARTIGO</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block en-only">LATEST POST</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block fr-only">DERNIER POST</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block es-only">ÚLTIMO POST</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block ja-only">最新の投稿</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block zh-only">最新帖子</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block de-only">LETZTER BEITRAG</span>
              <span class="text-xs font-mono text-neonCyan mb-1 block ru-only">ПОСЛЕДНИЙ ПОСТ</span>
              <h3 class="font-bold text-sm mb-2 mt-2 pt-only">{latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 en-only">{latestAcademic.data.titleEn || latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 fr-only">{latestAcademic.data.titleFr || latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 es-only">{latestAcademic.data.titleEs || latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 ja-only">{latestAcademic.data.titleJa || latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 zh-only">{latestAcademic.data.titleZh || latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 de-only">{latestAcademic.data.titleDe || latestAcademic.data.title}</h3>
              <h3 class="font-bold text-sm mb-2 mt-2 ru-only">{latestAcademic.data.titleRu || latestAcademic.data.title}</h3>
            </div>
            <div class="mt-4 flex items-end justify-between">
              <span class="text-xs text-textMuted font-mono pt-only">{latestAcademic.data.semester} | {latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono en-only">{latestAcademic.data.semester} | {latestAcademic.data.courseEn || latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono fr-only">{latestAcademic.data.semester} | {latestAcademic.data.courseFr || latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono es-only">{latestAcademic.data.semester} | {latestAcademic.data.courseEs || latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono ja-only">{latestAcademic.data.semester} | {latestAcademic.data.courseJa || latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono zh-only">{latestAcademic.data.semester} | {latestAcademic.data.courseZh || latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono de-only">{latestAcademic.data.semester} | {latestAcademic.data.courseDe || latestAcademic.data.course}</span>
              <span class="text-xs text-textMuted font-mono ru-only">{latestAcademic.data.semester} | {latestAcademic.data.courseRu || latestAcademic.data.course}</span>
            </div>
          </div>
        ) : (
          <div class="flex flex-col h-full justify-center items-center">
             <svg class="w-8 h-8 text-surfaceHighlight group-hover:text-neonCyan transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5zm0 0v7"></path></svg>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain pt-only">ACESSAR ARQUIVO</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain en-only">ACCESS ARCHIVE</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain fr-only">ACCÉDER AUX ARCHIVES</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain es-only">ACCEDER AL ARCHIVO</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ja-only">アーカイブにアクセス</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain zh-only">访问存档</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain de-only">ARCHIV AUFRUFEN</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ru-only">ДОСТУП К АРХИВУ</span>
          </div>
        )}
      </BentoBox>

      <!-- Blog Box -->
      <BentoBox spanCol="col-span-4 md:col-span-2" spanRow="row-span-1" glowColor="lime" href="/blog">
        {latestBlog ? (
          <div class="flex flex-col h-full justify-between">
            <div>
              <span class="text-xs font-mono text-neonLime mb-1 block pt-only">ÚLTIMO POST DO BLOG</span>
              <span class="text-xs font-mono text-neonLime mb-1 block en-only">LATEST BLOG POST</span>
              <span class="text-xs font-mono text-neonLime mb-1 block fr-only">DERNIER ARTICLE DE BLOG</span>
              <span class="text-xs font-mono text-neonLime mb-1 block es-only">ÚLTIMO POST DEL BLOG</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ja-only">最新のブログ記事</span>
              <span class="text-xs font-mono text-neonLime mb-1 block zh-only">最新博客文章</span>
              <span class="text-xs font-mono text-neonLime mb-1 block de-only">LETZTER BLOGBEITRAG</span>
              <span class="text-xs font-mono text-neonLime mb-1 block ru-only">ПОСЛЕДНИЙ ПОСТ В БЛОГЕ</span>
              <h3 class="font-bold text-lg mb-2 mt-2 pt-only">{latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 en-only">{latestBlog.data.titleEn || latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 fr-only">{latestBlog.data.titleFr || latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 es-only">{latestBlog.data.titleEs || latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 ja-only">{latestBlog.data.titleJa || latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 zh-only">{latestBlog.data.titleZh || latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 de-only">{latestBlog.data.titleDe || latestBlog.data.title}</h3>
              <h3 class="font-bold text-lg mb-2 mt-2 ru-only">{latestBlog.data.titleRu || latestBlog.data.title}</h3>
              <p class="text-sm text-textMuted line-clamp-2 pt-only">{latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 en-only">{latestBlog.data.descriptionEn || latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 fr-only">{latestBlog.data.descriptionFr || latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 es-only">{latestBlog.data.descriptionEs || latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 ja-only">{latestBlog.data.descriptionJa || latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 zh-only">{latestBlog.data.descriptionZh || latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 de-only">{latestBlog.data.descriptionDe || latestBlog.data.description}</p>
              <p class="text-sm text-textMuted line-clamp-2 ru-only">{latestBlog.data.descriptionRu || latestBlog.data.description}</p>
            </div>
            <div class="mt-4 flex items-end justify-between">
              <span class="text-xs text-textMuted font-mono pt-only">{latestBlog.data.date.toLocaleDateString('pt-BR')}</span>
              <span class="text-xs text-textMuted font-mono en-only">{latestBlog.data.date.toLocaleDateString('en-US')}</span>
              <span class="text-xs text-textMuted font-mono fr-only">{latestBlog.data.date.toLocaleDateString('fr-FR')}</span>
              <span class="text-xs text-textMuted font-mono es-only">{latestBlog.data.date.toLocaleDateString('es-ES')}</span>
              <span class="text-xs text-textMuted font-mono ja-only">{latestBlog.data.date.toLocaleDateString('ja-JP')}</span>
              <span class="text-xs text-textMuted font-mono zh-only">{latestBlog.data.date.toLocaleDateString('zh-CN')}</span>
              <span class="text-xs text-textMuted font-mono de-only">{latestBlog.data.date.toLocaleDateString('de-DE')}</span>
              <span class="text-xs text-textMuted font-mono ru-only">{latestBlog.data.date.toLocaleDateString('ru-RU')}</span>
              
              <span class="text-xs font-mono text-neonLime pt-only">VER BLOG &rarr;</span>
              <span class="text-xs font-mono text-neonLime en-only">VIEW BLOG &rarr;</span>
              <span class="text-xs font-mono text-neonLime fr-only">VOIR BLOG &rarr;</span>
              <span class="text-xs font-mono text-neonLime es-only">VER BLOG &rarr;</span>
              <span class="text-xs font-mono text-neonLime ja-only">ブログを見る &rarr;</span>
              <span class="text-xs font-mono text-neonLime zh-only">查看博客 &rarr;</span>
              <span class="text-xs font-mono text-neonLime de-only">BLOG ANSEHEN &rarr;</span>
              <span class="text-xs font-mono text-neonLime ru-only">СМОТРЕТЬ БЛОГ &rarr;</span>
            </div>
          </div>
        ) : (
          <div class="flex flex-col h-full justify-center items-center group">
             <svg class="w-8 h-8 text-surfaceHighlight group-hover:text-neonLime transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2.5 2.5 0 00-2.5-2.5H15M9 11l3 3m0 0l3-3m-3 3V8"></path></svg>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain pt-only">VER BLOG</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain en-only">VIEW BLOG</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain fr-only">VOIR BLOG</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain es-only">VER BLOG</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ja-only">ブログを見る</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain zh-only">查看博客</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain de-only">BLOG ANSEHEN</span>
             <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ru-only">СМОТРЕТЬ БЛОГ</span>
          </div>
        )}
      </BentoBox>

      <!-- View All Posts Box -->
      <BentoBox spanCol="col-span-4 md:col-span-2" spanRow="row-span-1" glowColor="cyan" href="/posts" className="group">
        <div class="flex flex-col h-full justify-center items-center">
           <svg class="w-8 h-8 text-surfaceHighlight group-hover:text-neonCyan transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain pt-only">VER TODOS OS POSTS</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain en-only">VIEW ALL POSTS</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain fr-only">VOIR TOUS LES POSTS</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain es-only">VER TODOS LOS POSTS</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ja-only">すべての投稿を見る</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain zh-only">查看所有文章</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain de-only">ALLE BEITRÄGE ANSEHEN</span>
           <span class="text-xs font-mono text-textMuted mt-2 group-hover:text-textMain ru-only">СМОТРЕТЬ ВСЕ ПОСТЫ</span>
        </div>
      </BentoBox>

    </div>
  </main>
</Layout>
